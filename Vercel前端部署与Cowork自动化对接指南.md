# Vercel 前端部署 + Cowork 自动化对接指南

> 本文档是《竞品分析 Agent 搭建攻略》的补充。
> 前三步（注册 Meta → 写脚本 → 本地测试）按原文档做，
> 从这里开始是做前端展示 + 接入 Cowork 自动分析。

---

## 一、整体架构（你、我、Vercel 怎么配合）

```
你对我说："分析 Last War"
       │
       ▼
┌──────────────────────────────────┐
│       我（Claude Cowork）         │
│  ① 跑 collect_ads.py 拉 Meta 数据 │
│  ② 调 DeepSeek API 分析每条广告   │
│  ③ 生成 data.json 写出到你的目录   │
└──────────────┬───────────────────┘
               │ file saved
               ▼
┌──────────────────────────────────┐
│   你的电脑 — 项目文件夹            │
│   data/Last_War_ads_20260524.json │
│   data/analysis_summary.json      │
└──────────────┬───────────────────┘
               │ "git push"
               ▼
┌──────────────────────────────────┐
│   GitHub 仓库                     │
│   Vercel 检测到更新 → 自动部署     │
└──────────────┬───────────────────┘
               │
               ▼
┌──────────────────────────────────┐
│   面试官打开网站 → 看到最新分析    │
└──────────────────────────────────┘
```

### 你在整个流程里只需要做的

| 时机 | 你要做的事 |
|------|-----------|
| **任何时候** | 在 Cowork 对我说"分析 XX" |
| **我分析完后** | 打开 VSCode → Source Control → 写 commit → push |
| **第一次搭建** | Meta 注册、建仓库、连 Vercel、授权文件夹 |

---

## 二、项目结构（你的 GitHub 仓库长这样）

```
ad-analyzer/
├── index.html          # Vercel 前端页面，展示分析结果
├── data/
│   └── .gitkeep        # 分析结果 JSON 存这里
├── scripts/
│   ├── collect_ads.py      # 采集脚本（原文档第四章）
│   ├── config.json         # 竞品配置
│   └── requirements.txt    # Python 依赖
├── vercel.json         # Vercel 部署配置
└── README.md           # 项目说明
```

### 各文件作用

| 文件 | 由谁生成 | 说明 |
|------|---------|------|
| `index.html` | **我来写** | 前端页面：输入框 + 图表 + 报告展示 |
| `collect_ads.py` | 你按原文档用 Claude Code 生成 | 调 Meta API 采集数据 |
| `config.json` | **我来生成模板，你填** | 竞品列表 + Meta Token |
| `vercel.json` | **我来写** | Vercel 纯静态部署配置 |

---

## 三、前端页面（index.html）

> 这个页面的代码我来写，你不用管。这里只告诉你它长什么样。

### 页面布局

```
┌──────────────────────────────────────────┐
│             竞品广告分析                   │
├──────────────────────────────────────────┤
│  [Last War ▼] [开始分析]                  │  ← 竞品选择 + 按钮
├──────────────────────────────────────────┤
│                                          │
│  素材类型分布       关键词 TOP 10          │
│  ┌──────────┐    ┌──────────────────┐    │
│  │  饼图     │    │  柱状图          │    │
│  │  视频75%  │    │  免费: 12次      │    │
│  │  图片20%  │    │  下载: 10次      │    │
│  │  轮播5%   │    │  礼包: 8次       │    │
│  └──────────┘    └──────────────────┘    │
│                                          │
│  📋 策略分析报告                          │
│  ┌──────────────────────────────────┐    │
│  │ 【本周策略重点】                   │    │
│  │ ...                              │    │
│  │ 【团队建议】                      │    │
│  │ ...                              │    │
│  └──────────────────────────────────┘    │
│                                          │
│  数据更新于：2026-05-24                    │
├──────────────────────────────────────────┤
│  工具说明 | 局限与计划                     │
└──────────────────────────────────────────┘
```

### 数据来源

页面读取 `data/` 目录下的 JSON 文件渲染图表。也就是说：
- 我分析完 → 写入 `data/` → git push 后 → 页面自动显示最新数据
- 面试官切换竞品名时，页面读取对应的 JSON 文件展示（预置好的）

---

## 四、Cowork 定时任务怎么配（到时候我帮你设）

### 4.1 你先把文件夹授权给我

在 Claude 桌面版里，你需要选择一个文件夹授权给我访问。操作路径：

```
Claude 桌面版设置 → 选择文件夹 → 选中你的项目目录 ad-analyzer/
```

授权之后，我就能读写你这个项目里的文件。

### 4.2 定时任务的完整 Prompt

到时候你在 Cowork 里跟我说：

> "帮我创建一个每周一定时任务，分析竞品广告数据"

我会设一个任务，内容如下：

```
【定时任务指令】

阶段一：数据采集
1. 读取 config.json 获取竞品列表和 Meta Token
2. 对每个竞品运行 python3 scripts/collect_ads.py --name "Last War" --token {TOKEN} --output ./data

阶段二：AI 分析
3. 读取生成的 JSON 数据
4. 对每条广告分析：素材类型、关键词、情感诉求、策略
5. 聚合生成 analysis_summary.json

阶段三：输出
6. 结果写入 data/ 目录
7. data.json 供前端页面读取展示
```

### 4.3 手动触发

除了定时任务，你随时可以对我说：

> "帮我分析一下 {竞品名}"

我会立刻跑一次，结果写到你的项目文件夹里。你看到文件变了，VSCode 点一下 push，网站就更新了。

---

## 五、你从今天开始的执行清单

### 第一批（你先做完）

| # | 事项 | 预计时间 |
|---|------|---------|
| 1 | 注册 Meta Developer 账号 | 10 分钟 |
| 2 | 创建 App，获取长期 Token | 10 分钟 |
| 3 | 用浏览器测试一次 Meta API 是否通 | 5 分钟 |
| 4 | 建一个 GitHub 仓库 `ad-analyzer`（空的就行） | 5 分钟 |
| 5 | 连上 Vercel（导入这个 GitHub 仓库） | 5 分钟 |
| 6 | 本地 clone 到电脑 | 2 分钟 |
| 7 | 安装 Python 依赖 | 5 分钟 |
| 8 | 在 Claude 桌面版授权这个文件夹给我 | 1 分钟 |

全部做完大概 **40 分钟**，然后告诉我"准备好了"，我来做接下来的事。

### 第二批（我来做）

| # | 事项 | 说明 |
|---|------|------|
| 9 | 写 index.html 前端页面 | 我来写，你放到项目根目录 |
| 10 | 写 vercel.json 部署配置 | 我来写 |
| 11 | 写 config.json 模板 | 我生成，你填 Meta Token 和 DeepSeek Key |
| 12 | 配 collect_ads.py 的输出路径 | 改成输出到 data/ 目录 |
| 13 | 在 Cowork 里设定时任务 | 我来设 |

### 第三批（你日常用）

| # | 事项 | 说明 |
|---|------|------|
| 14 | 对我说"分析一下 Last War" | 我实时跑，你推 GitHub → 网站更新 |
| 15 | 录屏提交 | 跑一轮全流程，录下来 |

---

## 六、提交时你的产出物

| # | 产出物 | 说明 |
|---|--------|------|
| 1 | **Vercel 网站链接** | 面试官打开能看到真实数据 |
| 2 | **Cowork 对话录屏** | 你对我说"分析 Last War"，我实时跑的全过程 |
| 3 | **网站操作录屏** | 打开网站，切换竞品，展示图表 + 报告 |
| 4 | **工具说明** | 用了 Claude Code（写脚本）、Claude Cowork（编排 + 定时）、DeepSeek（分析）、Vercel（前端） |
| 5 | **局限与计划** | 当前做到了什么，还差什么，打算怎么补 |

---

## 七、FAQ

**Q：定好目标竞品是哪几个？**
A：Last War、Monopoly Go!、Whiteout Survival、Royal Match

**Q：我什么时候能拿到前端页面代码？**
A：你把仓库建好、授权文件夹给我之后，我直接写进去。

**Q：每次分析完都要我手动 push？**
A：对，就 10 秒钟的事。VSCode 打开 → 点一下 → push。

**Q：如果我忘了 Meta Token 过期了怎么办？**
A：我跑的时候如果检测到 API 报错，会提醒你刷新 Token。你在 Meta 后台刷新一下就行。

---

> 把第一批的 8 件事做完，告诉我"准备好了"，剩下的事我来搞定。
